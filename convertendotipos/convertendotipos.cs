// Convertendo tipos
// Descrição

/*using System;

namespace MeuApp
{
    class Program
    {
        static void Main(string[] args)
        {
            int inteiro = 100;
            float real = 25.5f;

            real = inteiro;
            inteiro = (int)real;
            string valorReal = real.ToString();
            inteiro = int.Parse(real.ToString());
            inteiro = Convert.ToInt32(real);

            Console.WriteLine(inteiro);
            Console.WriteLine(Convert.ToBoolearn(255.of));
        }
    }
}*/   